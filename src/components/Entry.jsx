/*
const Entry = ({ entryData }) => {
    if (!entryData) {
        throw new Error("Entry component was not given any data");
    }

    return (<tr>
        <td>${ entryData.o }</td>
        <td>${ entryData.h }</td>
        <td>${ entryData.l }</td>
        <td>${ entryData.c }</td>
        <td>${ entryData.pc }</td>
        <td>{ entryData.t }</td>
    </tr>);
}

export default Entry; 
*/